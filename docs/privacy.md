# CircuitBoss Privacy Baseline

- API credentials must never be stored in source control, plaintext configuration, or logs.
- Project snapshots should be redacted before transmission by default.
- Absolute paths are treated as sensitive and should not leave the adapter boundary.
- Schematic images and datasheet-derived content should remain opt-in for future milestones.
